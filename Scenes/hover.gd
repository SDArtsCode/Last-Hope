extends AnimationPlayer

# Greg: Luke/Brenden I 'm taking the animation out just for testing rn, we can put this back in later.
#func _ready():
#	yield(get_tree().create_timer(randf()), "timeout")
#	play("hover")

